// environments/environment.ts
export const environment = {
  production: false,
  // This must match the server's PORT (5000) and API prefix (api/)
  // apiBaseUrl: 'http://localhost:5000/api/' 
  apiBaseUrl: 'https://trendify-api-sesi.onrender.com/api/'
};